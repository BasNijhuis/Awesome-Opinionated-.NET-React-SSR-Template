# Build results
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/

# Generated OpenAPI document (build artifact)
src/Services/Acme.Api/openapi/

# IDE
.vs/
*.user
*.suo
.idea/

# Frontend (pnpm workspace — node_modules hoists to the root and each member symlinks into it)
node_modules/
src/Services/acme-web/build/
src/Services/acme-web/.react-router/

# Playwright (tests/e2e)
playwright-report/
test-results/
blob-report/
.playwright/

# Environment
.env
.env.local
.env.*.local

# OS
Thumbs.db
.DS_Store

# Aspire
.aspire/

# Keep the root workspace lockfile (single source of truth for all JS members)
!pnpm-lock.yaml

# Transient lockfile generated by `dotnet run setup.cs` (the file-based setup script)
/packages.lock.json
