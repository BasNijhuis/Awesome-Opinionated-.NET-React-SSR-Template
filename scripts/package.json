{
  "name": "scripts",
  "private": true,
  "type": "module",
  "description": "Repo dev/build scripts (not part of the app runtime). Currently the web client's contract-type generator; add future repo tooling here.",
  "scripts": {
    "gen-contract": "tsx generate-contract-types.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
