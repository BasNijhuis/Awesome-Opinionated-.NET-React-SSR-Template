{
  "name": "scripts",
  "private": true,
  "type": "module",
  "description": "Repo dev/build scripts (not part of the app runtime). Currently the web client's API-type generator; add future repo tooling here.",
  "scripts": {
    "gen-api-types": "tsx generate-api-types.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
